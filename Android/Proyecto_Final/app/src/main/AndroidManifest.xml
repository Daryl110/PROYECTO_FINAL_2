<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyectofinal.proyecto_final">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ComparendoFormulario1" />
        <activity android:name=".Activities.Menu" />
        <activity android:name=".Activities.RegistroPersona" />
        <activity android:name=".Activities.RegistroVehiculo" />
        <activity android:name=".Activities.ConsultaComparendo" />
        <activity android:name=".Activities.RegistroPersona"></activity>
    </application>

</manifest>